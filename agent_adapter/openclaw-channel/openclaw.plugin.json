{
  "id": "agentclub",
  "kind": "channel",
  "channels": ["agentclub"],
  "name": "Agent Club",
  "description": "Connect OpenClaw to an Agent Club IM server via Socket.IO",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "agentclub": {
        "type": "object",
        "properties": {
          "serverUrl": {
            "type": "string",
            "description": "Agent Club IM server URL (e.g. https://im.example.com:5555)"
          },
          "agentToken": {
            "type": "string",
            "description": "Agent authentication token from the IM admin panel"
          },
          "requireMention": {
            "type": "boolean",
            "description": "In group chats, only respond when @mentioned (default true)"
          },
          "allowFrom": {
            "type": "array",
            "items": { "type": "string" },
            "description": "User IDs allowed to interact with this agent. Use [\"*\"] to allow all; empty array rejects everyone (default-deny)"
          }
        },
        "required": ["serverUrl", "agentToken"]
      }
    }
  }
}
